# 📖 Phonebook App (สมุดโทรศัพท์)

โปรเจกต์ฝึกเขียน Web Application แบบ Full-Stack ด้วย Python Flask และฐานข้อมูล SQLite
สามารถบันทึกชื่อและเบอร์โทรศัพท์ และเรียกดูข้อมูลได้

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
* **Back-end:** Python, Flask
* **Database:** SQLite
* **Front-end:** HTML, JavaScript (Fetch API)

## ✨ ฟีเจอร์หลัก (Features)
1.  **บันทึกเบอร์โทร (Create):** กรอกชื่อและเบอร์โทรผ่านหน้าเว็บเพื่อบันทึกลงฐานข้อมูล
2.  **ค้นหาเบอร์โทร (Read):** ค้นหาเบอร์โทรจากชื่อเพื่อนผ่าน API

## 🚀 วิธีติดตั้งและใช้งาน (How to Run)

1.  **ติดตั้งไลบรารีที่จำเป็น:**
    ```bash
    pip install flask
    ```

2.  **สร้างฐานข้อมูล (ครั้งแรก):**
    ```bash
    py init_db.py
    ```

3.  **รัน Server:**
    ```bash
    py app.py
    ```

4.  **ใช้งาน:**
    เปิด Browser แล้วเข้าไปที่ `http://127.0.0.1:5000/`

---
*Developed by Anusit-Ops*